import '../imports/api/slidervalues.js';

Meteor.startup(() => {
  // code to run on server at startup
});

// Meteor.methods({
//   removeAllUsers:function(){
//     if (Meteor.isServer) {
//     Meteor.startup(function() {
//       return Meteor.methods({
//         removeAllPosts: function() {
//           return SliderValues.remove({});
//         }
//       });
//     });
//   }
//   }
// });
